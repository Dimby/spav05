import React, { FC } from 'react'

interface GalleryModalProps {
    url: string
    title: string
    description: string
    date: string
}

const GalleryModal: FC<GalleryModalProps> = ({url, title, description, date}) => {
    return <></>
}

export default GalleryModal;